/* ============================================================
   Repositorio de la fuente "Coordenadas" (server/data/coordenadas.json).
   Instancia de create-data-source-store.js — ver ese archivo para la
   lógica real (genérica, comparte forma con inventory.store.js).
   ============================================================ */

module.exports = require('./create-data-source-store')('coordenadas');
