{
  "name": "gdapp",
  "version": "0.1.0",
  "private": true,
  "description": "GDapp — plataforma WEB (/desk) + PWA (/app) con acceso unificado",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
